/**
 * Replaces spaces with hyphens, split camelCase text,
 * remove non-word chars, remove accents and convert to lower case.
 */
export declare const hyphenate: (str: string, split?: string) => string;
