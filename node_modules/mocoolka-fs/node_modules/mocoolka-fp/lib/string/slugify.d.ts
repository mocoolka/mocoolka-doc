/**
 * Convert to lower case, remove accents, remove non-word chars and
 * replace spaces with the specified delimeter.
 * Does not split camelCase text.
 */
declare const slugify: (str: string, delimeter?: string) => string;
export { slugify };
