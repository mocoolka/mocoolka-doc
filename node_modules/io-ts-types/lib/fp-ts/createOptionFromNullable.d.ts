import * as t from 'io-ts';
import { Option } from 'fp-ts/lib/Option';
export declare class OptionFromNullableType<RT extends t.Any, A = any, O = A, I = t.mixed> extends t.Type<A, O, I> {
    readonly type: RT;
    readonly _tag: 'OptionFromNullableType';
    constructor(name: string, is: OptionFromNullableType<RT, A, O, I>['is'], validate: OptionFromNullableType<RT, A, O, I>['validate'], serialize: OptionFromNullableType<RT, A, O, I>['encode'], type: RT);
}
export declare const createOptionFromNullable: <RT extends t.Type<A, O, t.mixed>, A = any, O = A>(type: RT, name?: string) => OptionFromNullableType<RT, Option<RT["_A"]>, RT["_O"] | null, t.mixed>;
