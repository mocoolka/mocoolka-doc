"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
var t = require("io-ts");
var Either_1 = require("fp-ts/lib/Either");
var EitherFromJSONType = /** @class */ (function (_super) {
    __extends(EitherFromJSONType, _super);
    function EitherFromJSONType(name, is, validate, serialize, left, right) {
        var _this = _super.call(this, name, is, validate, serialize) || this;
        _this.left = left;
        _this.right = right;
        _this._tag = 'EitherFromJSONType';
        return _this;
    }
    return EitherFromJSONType;
}(t.Type));
exports.EitherFromJSONType = EitherFromJSONType;
function createEitherFromJSON(leftType, rightType, name) {
    if (name === void 0) { name = "Either<" + leftType.name + ", " + rightType.name + ">"; }
    var JSONLeft = t.type({
        type: t.literal('Left'),
        value: leftType
    });
    var JSONRight = t.type({
        type: t.literal('Right'),
        value: rightType
    });
    var JSONEither = t.taggedUnion('type', [JSONLeft, JSONRight]);
    return new EitherFromJSONType(name, function (m) {
        return (m instanceof Either_1.Right && rightType.is(m.value)) || (m instanceof Either_1.Left && leftType.is(m.value));
    }, function (m, c) {
        var validation = JSONEither.validate(m, c);
        if (validation.isLeft()) {
            return validation;
        }
        else {
            var e = validation.value;
            switch (e.type) {
                case 'Left':
                    return t.success(Either_1.left(e.value));
                case 'Right':
                    return t.success(Either_1.right(e.value));
            }
        }
    }, function (a) {
        return a.fold(function (l) { return ({ type: 'Left', value: leftType.encode(l) }); }, function (a) { return ({ type: 'Right', value: rightType.encode(a) }); });
    }, leftType, rightType);
}
exports.createEitherFromJSON = createEitherFromJSON;
//# sourceMappingURL=createEitherFromJSON.js.map